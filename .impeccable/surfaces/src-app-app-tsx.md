---
version: 1
slug: 'src-app-app-tsx'
primary_target: 'src/app/App.tsx'
related_targets:
  [
    'src/components/Landing.tsx',
    'src/components/Question.tsx',
    'src/components/Results.tsx',
    'src/components/ModifiersDialog.tsx',
    'src/styles.css',
  ]
---

Quizmon preserves its recognizable wordmark and crisp Pokémon-game landscape. The landing screen keeps its familiar open composition and uses the approved restrained Logo Echo treatment: one large cobalt Daily Challenge button is the primary action, while the two Training buttons remain smaller beneath it. The daily control contains its date without an outer card or nested button. After completion, the same footprint becomes a cream share button that shows the score. Controls borrow the wordmark's navy outline, cobalt fill, warm yellow lower edge, compact corners, and modest hard shadow. Avoid giant HUD frames, pixel fonts, glass panels, blurred scenery, and broad structural changes. Quiz states, results, and the modifiers dialog should feel like the same small game system without becoming a simulated handheld console. Motion is quick and physical through transform and opacity. The first viewport must match the approved comp at 1586 by 992 before responsive adaptation.
