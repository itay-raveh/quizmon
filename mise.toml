min_version = "2026.8.14"

[settings]
lockfile = true
lockfile_platforms = ["linux-x64", "macos-arm64"]

[tools]
actionlint = "1.7.12"
lychee = "0.24.2"
node = "24.20.0"
prek = "0.5.0"
shellcheck = "0.11.0"
"github:zizmorcore/zizmor" = "1.29.0"

[tasks.setup]
description = "Set up tools, dependencies, browser binaries, and Git hooks"
run = [
  "npm ci",
  "npx --no-install playwright install chromium webkit",
  "prek install",
]

[tasks.check]
description = "Run every repository check without modifying files"
run = [
  "npm run format:check",
  "npm run lint",
  "npm run check:dead-code",
  "npm test",
  "npm run build",
  "npm run test:e2e",
  "prek validate-config prek.toml",
  "npm run deploy:dry-run",
  "actionlint",
  "zizmor --offline .github/workflows",
  "lychee --offline --include-fragments --root-dir \"$PWD/dist\" dist/index.html",
  "lychee --offline --include-fragments README.md",
]

[tasks."badges:export"]
description = "Export trainer badge PNGs from the Aseprite master"
run = "aseprite --batch --script-param root=\"$PWD\" --script scripts/export-badges.lua"

[tasks.deploy]
description = "Build and deploy Quizmon to Cloudflare Workers"
run = [
  "npm run build",
  "npm run deploy",
]
